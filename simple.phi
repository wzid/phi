func fib(n: int): int {
    int a = 0;
    int b = 1;
    int i = 2;
    
    if (n < 2) {
        return n;
    }
    
    while (i < n + 1) {
        int temp = a + b;
        a = b;
        b = temp;
        i = i + 1;
    }
    
    return b;
}

func main() {
    int result = fibfibonacci(10);
    printf("Fib of 10 is %d\n", result);
    return 0;
}