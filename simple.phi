func get_age(): int => 25;

func main() {
    if (get_age() < 21) {
        int adjustment = 48;
        printf("No alcohol for you!\n");
    } else {
        printf("Bottles up!\n");
    }

    int adjustment = 90;
    if (get_age() + adjustment > 64) {
        printf("RETIREMENT IS FOR YOU SIR %d\n", adjustment);
    }

    return 0;
}